[project]
name = "github_bulk_repo_transfer"
version = "0.1.0"
description = "Automates moving every repository from one GitHub account to another "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
